APP_ENV=dev
DEBUG_MODE=true
DETAILED_LOGGING=false

# MongoDB
MONGO_USER=abdullahyousaf132
MONGO_PASSWORD=abd@1234
MONGO_HOST=cybersentinel-dev.rqqdg1u.mongodb.net
MONGO_DB=cybersentinel

# Auth
CS_API_ACCESS_TOKEN=your_token_here
JWT_SECRET=dev-secret-12345678
JWT_ALGORITHM=HS256
JWT_EXP_MINUTES=1440

# Kafka (disabled)
KAFKA_ENABLED=false
KAFKA_BOOTSTRAP_SERVERS=
KAFKA_TOPIC=
KAFKA_GROUP_ID=

# ML Model
MODEL_DIR=app/ml/models
MODEL_INTEGRITY_REQUIRED=true
ANOMALY_SCORE_THRESHOLD=0.65
# Optional external model API base URL (example: http://136.109.88.23:8000)
MODEL_API_URL=
# Timeout for external model API requests in seconds (example: 10)
MODEL_API_TIMEOUT_SECONDS=10

# Wazuh raw archive forwarder (POST /api/raw_wazuh_logs, header x-ingestion-key)
WAZUH_INGEST_KEY=
# Ubuntu forwarder runtime vars (set on Wazuh machine shell/service)
# CS_BACKEND_URL=http://<windows-backend-ip>:8000
# WAZUH_ARCHIVES_PATH=/var/ossec/logs/archives/archives.json
# WAZUH_FORWARDER_OFFSET_PATH defaults to ~/.cybersentinel-wazuh-forwarder.offset.json (no root). Optional: /var/lib/... for systemd as root
# WAZUH_FORWARDER_POLL_SEC=4

# Agent
AGENT_SERVICE_URL=
AGENT_TIMEOUT_SECONDS=10

# CORS
CORS_ALLOW_ORIGINS=*

# Frontend
FRONTEND_BASE_URL=http://localhost:3000

# Email (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=abdullahyousaf132@gmail.com
SMTP_PASSWORD=wxwjrdywuthoaieb
SMTP_USE_TLS=true
SMTP_USE_SSL=false
EMAIL_FROM=abdullahyousaf132@gmail.com

# Email TTLs
EMAIL_VERIFY_TTL_MINUTES=1440
PASSWORD_RESET_TTL_MINUTES=15